# Python version is pinned in .python-version, and it matters here. Render now defaults
# to 3.14, for which pydantic-core has no prebuilt wheel — pip then tries to compile it
# from Rust source and fails, because Render's build sandbox has a read-only cargo
# registry. The error names pydantic-core, but the cause is the interpreter.
fastapi==0.115.6
uvicorn[standard]==0.34.0
# supabase 2.31 brings realtime 2.31, which requires pydantic >= 2.11.7. Pinning 2.10.4
# made the whole set unresolvable. These three versions were resolved together rather
# than bumped one at a time.
pydantic==2.13.4
pydantic-settings==2.15.0
anthropic==0.42.0
# 2.11 predates Supabase's new API key format: it validates that the key looks like a
# JWT, so an sb_secret_... key is rejected as "Invalid API key" before any request is
# made. The legacy service_role JWT still works, but it is being phased out, so the
# client is upgraded rather than the key downgraded.
supabase==2.31.0
openpyxl==3.1.5
xlrd==2.0.1
beautifulsoup4==4.12.3
lxml==5.3.0
python-multipart==0.0.20
