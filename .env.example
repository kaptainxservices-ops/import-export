# Copy to .env and fill in. .env is gitignored — never commit real values.

# --- App ---
APP_ENV=development
LOG_LEVEL=INFO

# Shared secret n8n sends in the X-Ingest-Token header.
# /ingest is a public URL: without this, anyone who finds it can post fake offers.
# Generate one with:  python -c "import secrets; print(secrets.token_urlsafe(32))"
INGEST_TOKEN=

# --- Anthropic ---
# Set a spend limit in the Anthropic console BEFORE this key is ever used.
ANTHROPIC_API_KEY=
CLASSIFY_MODEL=claude-haiku-4-5-20251001
EXTRACT_MODEL=claude-sonnet-5

# --- Supabase ---
SUPABASE_URL=
# Service role key: bypasses row-level security. Render only.
# Never in the frontend, never in git, never in a browser.
SUPABASE_SERVICE_ROLE_KEY=

# --- Monitoring (Phase 7) ---
SENTRY_DSN=
